# OrgFlow Studio — Backup Directory

This directory is designated for local pre-sync snapshots, database dump archives, and recovery packages.

### SECURITY NOTICE:
All actual backup archives (*.json, *.db, *.tar.gz, *.zip) in this directory are strictly ignored by `.gitignore` and must NEVER be committed or pushed to any remote repository.
